---
description: Extract SVG or explicitly export PNG from a Diagrammatical deliverable
argument-hint: "[diagram path] [svg|png]"
---

Apply the shared workflow in `skills/diagrammatical/SKILL.md` in export mode using `$ARGUMENTS`.

Create PNG only when explicitly requested. If optional export dependencies are unavailable, give actionable installation guidance without blocking ordinary HTML, SVG, or validation work.

