---
description: Restyle an existing diagram without silently changing its meaning
argument-hint: "[diagram path and style request]"
---

Apply the shared workflow in `skills/diagrammatical/SKILL.md` in restyle mode using `$ARGUMENTS`.

Update presentation intent before regenerating deliverables. Preserve stable concept IDs and distinguish one-off diagram overrides from reusable brand changes.

