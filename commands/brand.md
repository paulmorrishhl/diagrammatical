---
description: Configure or revise project-owned Diagrammatical branding
argument-hint: "[brand request or source]"
---

Apply the shared workflow in `skills/diagrammatical/SKILL.md` in brand-onboarding mode using `$ARGUMENTS`.

Keep all user-owned configuration under `.diagrammatical/` in the user's project. Inspect candidate brand sources, explain ambiguous semantic mappings, and obtain approval before saving a reusable brand pack.

