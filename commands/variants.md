---
description: Generate materially different compositions for an existing diagram
argument-hint: "[diagram path or requested alternatives]"
---

Apply the shared workflow in `skills/diagrammatical/SKILL.md` in variants mode using `$ARGUMENTS`.

Hold semantic content constant, vary composition rather than colour alone, explain the communication trade-offs, and recommend one canonical option.

